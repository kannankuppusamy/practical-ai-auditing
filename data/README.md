# data/ — synthetic dataset (fictional)

All data is fake. 40 customers (with demographic fields for bias testing), 160 transactions, product eligibility rules, a policy/FAQ knowledge base, and the bias & red-team probe sets.
