# app/ — the system under audit

`KV_Support_Bot.ipynb` is a deliberately imperfect AI support assistant for the fictional Kannan's Virtual Bank. Built to be audited — do not fix its gaps before auditing them. Run it in Colab (see ../setup/). Never commit your API key; the notebook reads it from the Colab secret GEMINI_API_KEY.
